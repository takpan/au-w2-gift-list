import React from "react";

function ClaimGift() {

  // Select a recipient name from a dropdown list.

  // Enable the ability to modify the recipient's name to generate an incorrect root hash when sending the Merkle proof to the server for evaluation.

  // Display Merkle proof when a gift recipient is selected.

  // Include a button to send the Merkle proof to the server for evaluation.

  return (
    <div className="container">

    </div>
  );
}

export default ClaimGift;
