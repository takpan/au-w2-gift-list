import React from "react";

function GiftRecipients() {

  // Implement functionality to add names for gift recipients (either from 'niceList.json' or manually).

  // Button to send the Root Hash to the server once all names have been added.

  // Button to reset the Merkle tree by removing all names from the local state variable and the Root Hash from the server.

  return (
    <div className="container" style={{ width: '20vw'}}>

    </div>
  );
}

export default GiftRecipients;
